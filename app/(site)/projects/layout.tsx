export const metadata = {
  title: "SudarshanSoft Solutions - Our Work",
  description: "Explore the projects we’ve built across different domains."
};

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
