export const metadata = {
  title: "SudarshanSoft Solutions - About us",
  description: "About us"
};

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
